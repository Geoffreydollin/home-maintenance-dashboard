# Normalize line endings. Shell scripts and systemd units MUST be LF or they
# break when run on the Raspberry Pi (Linux), even if edited on Windows.
* text=auto eol=lf
*.sh text eol=lf
*.service text eol=lf

# Binary-ish / leave alone
*.ico binary
*.png binary
*.jpg binary
